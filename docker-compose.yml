services:
  atom:
    build:
      context: .
      dockerfile: Dockerfile
    image: atom:local
    container_name: atom-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      GIN_MODE: release
      PORT: "8080"
